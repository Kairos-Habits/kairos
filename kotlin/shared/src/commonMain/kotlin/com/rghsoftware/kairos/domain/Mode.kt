package com.rghsoftware.kairos.domain

/**
 * Represents the context in which tasks are performed.
 * Tasks are assigned to either LEAVING or ARRIVING mode.
 */
enum class Mode {
    LEAVING,
    ARRIVING,
}
